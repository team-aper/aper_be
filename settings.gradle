rootProject.name = 'aper_be'

// aper-submodule 추가 (Composite Build)
includeBuild '../aper-submodule'

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}
